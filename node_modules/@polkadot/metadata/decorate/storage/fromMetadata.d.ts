import { Registry } from '@polkadot/types/types';
import { Storage } from '../types';
import { Metadata } from '../../Metadata';
/** @internal */
export declare function storageFromMeta(registry: Registry, metadata: Metadata): Storage;
