import { Registry } from '@polkadot/types/types';
import { Constants } from '../types';
import { Metadata } from '../../Metadata';
/** @internal */
export declare function constantsFromMeta(registry: Registry, metadata: Metadata): Constants;
