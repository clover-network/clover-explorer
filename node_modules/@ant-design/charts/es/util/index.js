import { isType, clone, hasPath, setPath } from './utils';
import { getChart } from './getChart';
var utils = {
    isType: isType,
    clone: clone,
    hasPath: hasPath,
    setPath: setPath,
};
export { utils, getChart };
