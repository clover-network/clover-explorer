import React from 'react';
import { LineOptions as G2plotProps } from '@antv/g2plot';
import { ContainerProps } from '../hooks/useChart';
import { ChartRefOptions } from '../interface';
export interface LineConfig extends G2plotProps, ContainerProps {
    chartRef?: ChartRefOptions;
}
declare const LineChart: React.ForwardRefExoticComponent<LineConfig & React.RefAttributes<unknown>>;
export default LineChart;
