import React from 'react';
import { FunnelOptions as G2plotProps } from '@antv/g2plot';
import { ContainerProps } from '../hooks/useChart';
import { ChartRefOptions } from '../interface';
export interface FunnelConfig extends G2plotProps, ContainerProps {
    chartRef?: ChartRefOptions;
}
declare const FunnelChart: React.ForwardRefExoticComponent<FunnelConfig & React.RefAttributes<unknown>>;
export default FunnelChart;
