import React from 'react';
import { LiquidOptions as G2plotProps } from '@antv/g2plot';
import { ContainerProps } from '../hooks/useChart';
import { ChartRefOptions } from '../interface';
export interface LiquidConfig extends G2plotProps, ContainerProps {
    chartRef?: ChartRefOptions;
}
declare const LiquidChart: React.ForwardRefExoticComponent<LiquidConfig & React.RefAttributes<unknown>>;
export default LiquidChart;
