import React from 'react';
import { WaterfallOptions as G2plotProps } from '@antv/g2plot';
import { ContainerProps } from '../hooks/useChart';
import { ChartRefOptions } from '../interface';
export interface WaterfallConfig extends G2plotProps, ContainerProps {
    chartRef?: ChartRefOptions;
}
declare const WaterfallChart: React.ForwardRefExoticComponent<WaterfallConfig & React.RefAttributes<unknown>>;
export default WaterfallChart;
