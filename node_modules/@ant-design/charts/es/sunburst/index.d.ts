import React from 'react';
import { SunburstOptions as G2plotProps } from '@antv/g2plot';
import { ContainerProps } from '../hooks/useChart';
import { ChartRefOptions } from '../interface';
export interface SunburstConfig extends G2plotProps, ContainerProps {
    chartRef?: ChartRefOptions;
}
declare const SunburstChart: React.ForwardRefExoticComponent<SunburstConfig & React.RefAttributes<unknown>>;
export default SunburstChart;
