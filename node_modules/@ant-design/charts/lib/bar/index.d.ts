import React from 'react';
import { BarOptions as G2plotProps } from '@antv/g2plot';
import { ContainerProps } from '../hooks/useChart';
import { ChartRefOptions } from '../interface';
export interface BarConfig extends G2plotProps, ContainerProps {
    chartRef?: ChartRefOptions;
}
declare const BarChart: React.ForwardRefExoticComponent<BarConfig & React.RefAttributes<unknown>>;
export default BarChart;
