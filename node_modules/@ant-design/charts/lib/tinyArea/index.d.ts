import React from 'react';
import { TinyAreaOptions as G2plotProps } from '@antv/g2plot';
import { ContainerProps } from '../hooks/useChart';
import { ChartRefOptions } from '../interface';
export interface TinyAreaConfig extends G2plotProps, ContainerProps {
    chartRef?: ChartRefOptions;
}
declare const TinyAreaChart: React.ForwardRefExoticComponent<TinyAreaConfig & React.RefAttributes<unknown>>;
export default TinyAreaChart;
