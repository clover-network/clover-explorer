import React from 'react';
import { ColumnOptions as G2plotProps } from '@antv/g2plot';
import { ContainerProps } from '../hooks/useChart';
import { ChartRefOptions } from '../interface';
export interface ColumnConfig extends G2plotProps, ContainerProps {
    chartRef?: ChartRefOptions;
}
declare const ColumnChart: React.ForwardRefExoticComponent<ColumnConfig & React.RefAttributes<unknown>>;
export default ColumnChart;
