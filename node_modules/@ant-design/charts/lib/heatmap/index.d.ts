import React from 'react';
import { HeatmapOptions as G2plotProps } from '@antv/g2plot';
import { ContainerProps } from '../hooks/useChart';
import { ChartRefOptions } from '../interface';
export interface HeatmapConfig extends G2plotProps, ContainerProps {
    chartRef?: ChartRefOptions;
}
declare const HeatmapChart: React.ForwardRefExoticComponent<HeatmapConfig & React.RefAttributes<unknown>>;
export default HeatmapChart;
