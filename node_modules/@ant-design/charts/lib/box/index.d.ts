import React from 'react';
import { BoxOptions as G2plotProps } from '@antv/g2plot';
import { ContainerProps } from '../hooks/useChart';
import { ChartRefOptions } from '../interface';
export interface BoxConfig extends G2plotProps, ContainerProps {
    chartRef?: ChartRefOptions;
}
declare const BoxChart: React.ForwardRefExoticComponent<BoxConfig & React.RefAttributes<unknown>>;
export default BoxChart;
