import React from 'react';
import { PieOptions as G2plotProps } from '@antv/g2plot';
import { ContainerProps } from '../hooks/useChart';
import { ChartRefOptions } from '../interface';
export interface PieConfig extends G2plotProps, ContainerProps {
    chartRef?: ChartRefOptions;
}
declare const PieChart: React.ForwardRefExoticComponent<PieConfig & React.RefAttributes<unknown>>;
export default PieChart;
