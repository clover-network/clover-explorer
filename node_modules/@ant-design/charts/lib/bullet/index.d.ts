import React from 'react';
import { BulletOptions as G2plotProps } from '@antv/g2plot';
import { ContainerProps } from '../hooks/useChart';
import { ChartRefOptions } from '../interface';
export interface BulletConfig extends Omit<G2plotProps, 'color' | 'label' | 'style'>, ContainerProps {
    chartRef?: ChartRefOptions;
}
declare const BulletChart: React.ForwardRefExoticComponent<BulletConfig & React.RefAttributes<unknown>>;
export default BulletChart;
