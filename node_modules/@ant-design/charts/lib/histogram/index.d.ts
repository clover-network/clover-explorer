import React from 'react';
import { HistogramOptions as G2plotProps } from '@antv/g2plot';
import { ContainerProps } from '../hooks/useChart';
import { ChartRefOptions } from '../interface';
export interface HistogramConfig extends G2plotProps, ContainerProps {
    chartRef?: ChartRefOptions;
}
declare const HistogramChart: React.ForwardRefExoticComponent<HistogramConfig & React.RefAttributes<unknown>>;
export default HistogramChart;
