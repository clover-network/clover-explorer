import React from 'react';
import { RoseOptions as G2plotProps } from '@antv/g2plot';
import { ContainerProps } from '../hooks/useChart';
import { ChartRefOptions } from '../interface';
export interface RoseConfig extends G2plotProps, ContainerProps {
    chartRef?: ChartRefOptions;
}
declare const RoseChart: React.ForwardRefExoticComponent<RoseConfig & React.RefAttributes<unknown>>;
export default RoseChart;
