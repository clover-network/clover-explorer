import React from 'react';
import { TinyLineOptions as G2plotProps } from '@antv/g2plot';
import { ContainerProps } from '../hooks/useChart';
import { ChartRefOptions } from '../interface';
export interface TinyLineConfig extends G2plotProps, ContainerProps {
    chartRef?: ChartRefOptions;
}
declare const TinyLineChart: React.ForwardRefExoticComponent<TinyLineConfig & React.RefAttributes<unknown>>;
export default TinyLineChart;
