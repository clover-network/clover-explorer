import React from 'react';
import { RadarOptions as G2plotProps } from '@antv/g2plot';
import { ContainerProps } from '../hooks/useChart';
import { ChartRefOptions } from '../interface';
export interface RadarConfig extends G2plotProps, ContainerProps {
    chartRef?: ChartRefOptions;
}
declare const RadarChart: React.ForwardRefExoticComponent<RadarConfig & React.RefAttributes<unknown>>;
export default RadarChart;
