import { Data } from '../../types';
/**
 * 对数据进行百分比化
 * @param data
 * @param measure
 * @param groupField
 * @param as
 */
export declare function percent(data: Data, measure: string, groupField: string, as: string): any[];
