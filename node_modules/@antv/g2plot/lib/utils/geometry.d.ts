import { Chart, Geometry, View } from '@antv/g2';
import Element from '@antv/g2/lib/geometry/element';
/**
 * 在 Chart 中查找第一个指定 type 类型的 geometry
 * @param chart
 * @param type
 */
export declare function findGeometry(chart: View, type: string): Geometry;
/**
 * 获取 Chart 的 所有 elements
 */
export declare function getAllElements(chart: Chart): Element[];
