"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.transformDataToSankey = void 0;
/**
 * 将数据序列转换成 sankey layout 需要的数据结构
 * 1. 过滤掉处理来源去向相同的节点
 * 2. 避免形成环
 * @param data
 * @param sourceField
 * @param targetField
 * @param weightField
 */
function transformDataToSankey(data, sourceField, targetField, weightField) {
    if (!Array.isArray(data)) {
        return {
            nodes: [],
            links: [],
        };
    }
    var nodes = [];
    var links = [];
    // TODO 逍为
    // 1. 使用 Map 进行一些性能优化（目前 includes，indexOf 会大量进行数组遍历）
    // 2. link 去重
    // 数组变换成 sankey layout 的数据结构
    data.forEach(function (datum) {
        var source = datum[sourceField];
        var target = datum[targetField];
        var weight = datum[weightField];
        // source node
        if (!nodes.includes(source)) {
            nodes.push(source);
        }
        // target node
        if (!nodes.includes(target)) {
            nodes.push(target);
        }
        // links
        links.push({
            source: nodes.indexOf(source),
            target: nodes.indexOf(target),
            value: weight,
        });
    });
    return {
        nodes: nodes.map(function (name) { return ({ name: name }); }),
        links: links,
    };
}
exports.transformDataToSankey = transformDataToSankey;
//# sourceMappingURL=data.js.map