import { Data } from '../../../types';
import { SankeyLayoutInputData } from '../../../utils/transform/sankey';
/**
 * 将数据序列转换成 sankey layout 需要的数据结构
 * 1. 过滤掉处理来源去向相同的节点
 * 2. 避免形成环
 * @param data
 * @param sourceField
 * @param targetField
 * @param weightField
 */
export declare function transformDataToSankey(data: Data, sourceField: string, targetField: string, weightField: string): SankeyLayoutInputData;
