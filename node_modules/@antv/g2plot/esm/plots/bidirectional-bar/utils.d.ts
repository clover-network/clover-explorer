import { Datum } from '../../types';
import { BidirectionalBarOptions } from '.';
declare type TransformData = {
    type: string;
    [key: string]: string | number;
}[];
/**
 * bidirectional-bar 处理数据
 * @param xField
 * @param yField
 * @param data
 */
export declare function transformData(xField: string, yField: string[], data: Datum): TransformData;
/**
 * 是否横向，默认空为横向
 * @param layout
 */
export declare function isHorizontal(layout: BidirectionalBarOptions['layout']): boolean;
/**
 * 多 view 进行同步 padding 的自定义逻辑
 * @param chart
 * @param views
 * @param p
 */
export declare function syncViewPadding(chart: any, views: any, p: any): void;
export {};
