import { Geometry } from '@antv/g2';
import { LineOption } from '@antv/g2/lib/interface';
import { Datum, Data } from '../../../types/common';
import { Params } from '../../../core/adaptor';
import { FunnelOptions } from '../types';
/**
 * 漏斗图通用geometry label
 * @param geometry 对应的 chart geometry
 */
export declare function geometryLabel(geometry: Geometry): (params: Params<FunnelOptions>) => Params<FunnelOptions>;
/**
 * 漏斗图通用转化率组件
 * @param getLineCoordinate 用于获取特定的 line 的位置及配置
 */
export declare function conversionTagComponent(getLineCoordinate: (datum: Datum, datumIndex: number, data: Data, initLineOption: Record<string, any>) => LineOption): (params: Params<FunnelOptions>) => Params<FunnelOptions>;
